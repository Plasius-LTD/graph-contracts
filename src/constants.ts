export const DEFAULT_SOFT_TTL_SECONDS = 30;
export const DEFAULT_HARD_TTL_SECONDS = 300;
export const DEFAULT_SCHEMA_VERSION = "1";
export const SUPPORTED_SCHEMA_VERSIONS = [DEFAULT_SCHEMA_VERSION] as const;
export const DEPRECATED_SCHEMA_VERSIONS: readonly string[] = [];
